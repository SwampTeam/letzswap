{% trans_default_domain 'registration' %}

{{ 'REGISTRATION.MAIL.TITLE'|trans }}

{{ 'REGISTRATION.MAIL.TEXT'|trans({'%name%': user.getUsername()}) }}

{{ 'REGISTRATION.MAIL.TXT.COPY_LINK'|trans }} {{ url('account_activation_token', {'token': user.getActivationToken()}) }}
