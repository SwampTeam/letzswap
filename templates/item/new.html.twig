{% extends 'base.html.twig' %}

{% block title %}{{ 'ITEM.NEW.PAGETITLE'|trans }}{% endblock %}

{% block body %}
    {#    TODO: Need to be add to CSS to fix bootstrap file imput bug #}
    <style>
        .custom-file-label {
            opacity: 0;
        }

        .custom-file-input {
            opacity: 1;
        }
    </style>



    <div class="new_item_page">
        <div>
            <h1 id="registration_title">{{ 'ITEM.NEW.TITLE'|trans }}</h1>
        </div>
    </div>
    <div>
        {{ form_start(form) }}

        {{ form_widget(form) }}
    </div>
    <div id="add_item_buttons" class="d-flex justify-content-between">
        <button class="button">Add Item</button>
    </div>



    {{ form_end(form) }}
{% endblock %}
