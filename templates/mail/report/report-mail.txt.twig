{{ 'REPORT.MAIL.TITLE'|trans({'%item%': item.getTitle()|default('Something')}) }}
        {{ user.getUsername()|default('someone') }}!

The {{ item.title }}, belonging to {{ item.user.username }}, has been reported by one of our users as being {{ givenReportReason }}.

The reporting user, {{ user.username }} has sent the following message:

"{{ textComingFromReportForm }}"